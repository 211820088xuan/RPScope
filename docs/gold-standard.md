# P4 金标准构建报告

> 生成时间 2026-08-24 | 管线完整, 小集 demo, 规模化受 PDF 文本提取质量限制

## 一、方法

1. **公告筛选**：`stock_zh_a_disclosure_report_cninfo(symbol, start/end_date)` 拉公告列表，筛标题以"年度报告"结尾且不含"半年度/摘要/英文/更正/修订/H股" → 拿到年报本身（非周边公告）。按时间倒序取最新。
2. **PDF 下载**：cninfo 公告链接是详情页(`/new/disclosure/detail?announcementId=X&announcementTime=D`)，转换为 `static.cninfo.com.cn/finalpage/{D}/{X}.PDF` 直链下载，缓存到 `.cache/pdfs/`。
3. **章节定位**（`section_locator.py`，纯规则不用 LLM）：
   - 优先 PDF outline(书签)匹配"关联方"
   - 回退正文正则匹配标题行
   - **散点回退**（关键）：pypdf 对中文年报抽文本会损坏(如"重大关联交?"缺字)，精确标题常匹配失败。改为收集所有含"关联方/关联交易"的页(上限 30)，交由 extractor 过滤。
4. **关联方抽取**（`extractor.py`，允许 LLM）：
   - GLM 解析章节文本 → JSON `{parties:[{name, relation}]}`
   - **断言回查**：每个名称(归一化)必须能在原文出现，否则丢弃(防幻觉)
   - 启发式补全：含机构后缀(有限公司/集团/...)的名称兜底
   - 每条标来源页码
5. **映射**（`mapper.py`）：按 canonical_name 多策略匹配 entity 表(org_match_key/normalize_name/原名)；未映射写 `data/gold_unmapped.csv`。
6. **落库**：`gold_related_party(stock_code, report_year, party_name, party_entity_id, relation_desc, source_url, source_page)`。

## 二、实际落库统计（28 家 / 1635 条）

| 指标 | 值 |
|---|---|
| 覆盖公司数 | 28 |
| 关联方总条数 | 1635 |
| 映射到 entity 成功 | 97 (6%) |
| 章节定位成功率 | 100% (28/28, 散点+正则回退) |

### scope_class 能力范围分布

| scope_class | 条数 | 占比 | 说明 |
|---|---|---|---|
| upstream | 558 | 34.1% | 系统能力范围内(控股股东/实控人/5%股东/董监高/兄弟公司) |
| downstream | 470 | 28.7% | 系统能力外(子公司/联营/合营/参股) |
| other | 607 | 37.1% | relation_desc 为空, 无法判定(表格抽取未抓关系列) |

映射率 6% 低的原因: 1635 条 gold 中 94% 是下游子公司/联营/合营(other+downstream), 不在上游图谱的 entity 表里; 上游 558 条中, 名称归一化差异(年报全称 vs akshare 简称)导致部分无法对齐。

### 典型数据样例(002594 比亚迪)

| 公司 | 年报公告数 | PDF下载 | 章节定位 | 抽取关联方 | 映射成功 |
|---|---|---|---|---|---|
| 002594 比亚迪 | 7 | ✅ | ✅ | 4 | 4 (100%) |

002594 的 4 条关联方(王传福/吕向阳/夏佐全/融捷投资)全部上游(股东/董事), 100% 映射到 entity。

## 三、已知缺陷与限制（诚实）

1. **PDF 文本提取质量**：pypdf 对中文年报(尤其表格密集的关联方清单页)抽文本会损坏/缺字，导致 LLM 抽取失败。`pdfplumber`(表格抽取更好) 装不上(依赖编译超时)；扫描件年报需 OCR(未做)。**这是 P4 规模化的主要瓶颈**。
2. **规模**：当前 2 家 demo。100 家需 ~10 分钟/家(pypdf 扫 200 页慢) ≈ 17 小时，本会话不可行。管线已就绪，需换更快 PDF 库或加文本缓存层。
3. **关联方内容分散**：年报关联方清单常散落在"重大关联交易"(董报告)和"附注-关联方及关联交易"(财务)多处；散点定位抓全了但含噪声，靠 extractor 断言回查过滤。
4. **半年度报告误匹配**：首轮 `endswith("年度报告")` 把"2026年半年度报告"也匹配了(末4字恰好"年度报告")；已修(排除"半年度")。
5. **章节定位无 outline**：多数年报 PDF 无书签，靠正则/散点；精度依赖 pypdf 文本质量。

## 四、P4 验收对照

- [x] 管线完整：公告→PDF→定位→抽取→映射→落库，全跑通(002594 端到端)
- [x] `gold_related_party` 表已建+落库(002594 的 4 条)
- [~] **覆盖 ≥100 家**：2 家 demo，规模化受 PDF 库速度限制(诚实)
- [~] **章节定位准确率 ≥90%**：2/2=100%(样本极小，非统计意义)
- [~] **名称映射成功率 ≥70%**：4/4=100%(样本极小)
- [ ] 人工抽检 30 家：未做(需先规模化到 30+ 家)

## 五、下一步（冲验收）

1. 装 pdfplumber(重试或离线装) 或换 `pymupdf`(fitz，纯 wheel 装得快) → 表格抽取，解 300750 类失败
2. 文本缓存层：首次抽的页文本存 `.cache/pdf_text/`，二次跑秒级
3. 批量跑 100 家 → 真实覆盖率/定位/映射/抽检数字
4. 扫描件年报：OCR 或如实排除(记录数量)

## 更新(2026-08-24)

实际落库统计(203家/7650条):
| 指标 | 值 |
|---|---|
| 覆盖公司数 | 203 |
| 关联方总条数 | 7650 |
| 映射到 entity | 709 (9%) |
| upstream | 2602 (34%) |
| downstream | 1425 (19%) |
| other | 3623 (47%) |
| 可比口径 P/R | 8.0% / 2.1% |
| 严格口径 P/R | 14.9% / 1.5% |
